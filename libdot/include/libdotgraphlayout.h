namespace GraphLayout {

    // return a textual representation of graph layout (graph described in dot
    // format)

    const char *getgraph(const char *str);
    const char *getgraphfile(const char *str);
};
